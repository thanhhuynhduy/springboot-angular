import { CommandBarActions } from './command-bar-actions.enum';

describe('CommandBarActions', () => {
  it('should create an instance', () => {
    expect(new CommandBarActions()).toBeTruthy();
  });
});
